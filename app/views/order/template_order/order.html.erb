<div class="content-heading">
  Order REPORT <%= @brand_name %> Up To <%= Date.today.strftime("%d %B %Y") %> for <%= @branch %> BRANCH
</div>
<!-- START widgets box-->
<!-- END widgets box-->
<div class="row">
  <!-- START dashboard main content-->
  <div class="col-lg-12">
    <div class="row">
      <div class="col-lg-6">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            Outstanding Order <%= @brand %> 
          </div>
          <div class="panel-body">
            <table id="datatable_customer" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Order No</th>
                  <th>Customer</th>
                  <th>Salesman</th>
                  <th>Promised Delivery</th>
                  <th>Exceeds</th>
                </tr>
              </thead>
              <tbody>
                <% @out_daily.each do |dp| %>
                <tr class="gradeX">
                  <td><%= dp.order_no %></td>
                  <td><%= dp.customer %></td>
                  <td><%= dp.salesman %></td>
                  <td><%= dp.promised_delivery.strftime("%d-%b-%y") %></td>
                  <td><%= dp.exceeds %> days</td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            <%= @brand %> Order Ready to Ship
          </div>
          <div class="panel-body">
            <table id="datatable_city" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Order No</th>
                  <th>Customer</th>
                  <th>Sales</th>
                  <th>Promised Delivery</th>
                </tr>
              </thead>
              <tbody>
                <% @pick_daily.each do |dp| %>
                <tr class="gradeX">
                  <td><%= dp.order_no %></td>
                  <td><%= dp.customer %></td>
                  <td><%= dp.salesman %></td>
                  <td><%= dp.promised_delivery.strftime("%d-%b-%y") %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-12">
    <div class="row">
      <div class="col-lg-6">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            Held Orders by C1 for brand <%= @brand %> 
          </div>
          <div class="panel-body">
            <table id="held_orders" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Order No</th>
                  <th>Ship To</th>
                  <th>Salesman</th>
                  <th>Order Date</th>
                </tr>
              </thead>
              <tbody>
                <% @held_order_credit.each do |hd| %>
                <tr class="gradeX">
                  <td><%= hd.order_no %></td>
                  <td><%= hd.customer %></td>
                  <td><%= hd.salesman %></td>
                  <td><%= hd.promised_delivery.strftime("%d-%b-%y") %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            Held Orders by C2 for brand <%= @brand %> 
          </div>
          <div class="panel-body">
            <table id="stock-table" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Order No</th>
                  <th>Ship To</th>
                  <th>Salesman</th>
                  <th>Order Date</th>
                </tr>
              </thead>
              <tbody>
                <% @held_order_approve.each do |hd| %>
                <tr class="gradeX">
                  <td><%= hd.order_no %></td>
                  <td><%= hd.customer %></td>
                  <td><%= hd.salesman %></td>
                  <td><%= hd.promised_delivery.strftime("%d-%b-%y") %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>